version: '3.8'

services:
  backend:
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - DEBUG=1

  frontend:
    command: npm run dev
    environment:
      - NODE_ENV=development
